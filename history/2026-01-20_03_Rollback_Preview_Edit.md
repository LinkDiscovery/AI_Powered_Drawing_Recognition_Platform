# Preview vs Edit 페이지 분리 롤백 (Rollback)

**날짜:** 2026-01-20
**작업자:** AI Assistant (Antigravity)
**관련 문서:** `2026-01-20_Separate_Preview_Edit.md` (분리 작업 시도 내역)

## 1. 개요 (Overview)
사용자의 요청으로 `PreviewPage`와 `EditPage`를 분리하는 작업을 진행했으나, **사용성 검토 결과 기존 방식(통합 페이지)이 더 낫다고 판단**되어 해당 작업을 롤백했습니다.

## 2. 롤백 작업 내역 (Revert Details)
이 저장은 "분리 작업"을 취소하고 시스템을 이전 상태로 되돌리는 것을 목적으로 합니다.

### Frontend
1.  **Router 복구 (`src/router.tsx`)**
    *   `/edit/:id` 라우트를 제거했습니다.
2.  **Dashboard 복구 (`src/pages/dashboard/UserDashboard.tsx`)**
    *   파일 리스트 클릭 시 이동 경로를 `/edit/{id}`에서 다시 **`/preview`**(기존 로직)로 변경했습니다.
    *   `useFiles().openSingleFile`을 사용하여 Context에 파일 정보를 담고 이동합니다.
    *   `UploadItem` 타입 및 `formatSize` 헬퍼 함수를 복구하여 타입 오류를 수정했습니다.
3.  **파일 삭제**
    *   `src/pages/edit/EditPage.tsx`: 파일 삭제됨.

### Backend
*   **유지:** `GET /api/files/{id}` 엔드포인트는 향후 활용 가능성이 높아 삭제하지 않고 유지했습니다.

## 3. 현재 시스템 상태 (Current Status)
*   **Preview:** 업로드 직후 및 대시보드 진입 시 모두 `PreviewPage`를 사용합니다.
*   **Rotate Issues:** 현재 이미지 회전 후 저장 시, 화면과 실제 파일의 회전값이 일치하지 않는 이슈가 있어 백엔드(`FileController.java`)에 디버그 로그를 추가해둔 상태입니다.

---
*Generated by Antigravity*
